#pragma once

#include "Value.hpp"

namespace mc {

  struct Function {

    typedef Value (*InternalFunc)(Array<Value>);

    InternalFunc interal_func;

    Value call(Array<Value> args) {
      return inernal_func(args);
    }

  };

}
